<select
    class="select2 form-input-field adf-grid-filter-value {{fieldClass}}"
    name="{{name}}" {{#if multiple}}multiple{{/if}}
        {{#each dataAttributes}}
            data-{{name}}="{{value}}"
        {{/each}}
    >
    {{#unless required}}
      <option></option>
    {{/unless}}
    {{#each data}}
      <option value="{{this.value}}" {{#if tooltip}} data-tooltip="{{tooltip}}" {{/if}} {{optionSelected value ../currentValue}}>{{#if this.label}}{{this.label}}{{else}}{{this.value}}{{/if}}</option>
    {{/each}}
  </select><!-- /.select2 -->
